#include <unistd.h>
/**
 * _putchar - writes the character c to stdout
 * Return: On success 1.
 * On error, return -1, and set errno approprattely
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}

